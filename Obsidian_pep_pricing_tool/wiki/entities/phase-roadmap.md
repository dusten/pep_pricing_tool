---
title: Phase Roadmap
type: entity
tags: [roadmap, phases, planning, backlog]
created: 2026-06-29
sources: [phase1-framework]
---

# Phase Roadmap

## Phase 1 — ✅ Complete + Deployed (2026-06-29)

Auth, quota, design system, SPA shell, deployment scripts (~52 files). Live at https://price.themightygroupbuy.com.

Built:
- Register / login / logout / email verify / forgot+reset password
- Opaque bearer token sessions (`pc_sessions`)
- Waitlist gate + referral codes
- Free-tier query quota (3 per 72h rolling window, keyed by filter_hash)
- Navy/gold Japanese-woodblock design system (CSS custom properties, light/dark/system theme)
- Vue 3 SPA with router, Pinia stores, AppLayout, Sidebar, TopBar
- Deployment: `setup.sh` (fresh EC2) + `add-price-site.sh` (add-on vhost)
- DB schema with full domain model (products, specs, vendors, prices, billing tables)

## Phase 2 — ✅ Mostly built (2026-07-01), Stripe intentionally excluded

- ~~Stripe checkout + billing portal~~ — **not built, backlogged on purpose** (user directive: no billing/Stripe work until later)
- ~~Webhook handler~~ / ~~Referral credit grant on first paid invoice~~ — depends on Stripe, same backlog
- Admin: users ✅, waitlist invite ✅, subscription list ✅ (read-only, no billing actions since Stripe isn't built), performance metrics ✅, backup ✅ (mysqldump + vendor_files → zip)
- Waitlist join endpoint (`/api/waitlist/join`) — already existed from 2026-06-30 session

## Phase 3 — ✅ Built (2026-07-01)

- Vendor file upload UI (admin) ✅ — `VendorsTab.vue`
- Claude extraction pipeline ✅ — PDF via document block, XLSX/CSV via a hand-rolled ZipArchive/SimpleXML text converter (no new dependency); model `claude-sonnet-5` default, `claude-opus-4-8` override
- Comparison table view (`/comparison`) ✅ — filter/sort/$-per-unit highlighting, free-tier query metering enforced
- CSV/XLSX export (Pro tier) ✅ — built 2026-07-04, see backlog #5 Resolved
- Full raw data export (Expert tier) ✅ — built 2026-07-04, see backlog #5 Resolved
- Admin: vendor management ✅, product catalogue + alias merge tool ✅, file processing queue ✅
- **Bonus, not originally scoped**: Calendar/price-history view (`/calendar`), mobile bottom nav, `/settings` page

## Backlog — SOURCE OF TRUTH (priority order)

> **This is the canonical project backlog.** `CLAUDE.md` points here — add, re-prioritize,
> and mark items done *here*, not there. Item numbers are stable references (commit messages,
> memory, and analyses cite "#2", "#10", etc.), so retire an item by striking it, not renumbering.
> The wiki vault is tracked in git (only `.obsidian/` editor state is ignored), so this list
> travels with the repo — version-controlled, backed up, and available to fresh clones and cloud agents.

1. ~~**Spec sorting**~~ — **RESOLVED 2026-07-03** (see Resolved below).
2. ~~**`price_per_unit` kit-vial-count factor — write-path fix + all-vendors backfill.**~~ — **RESOLVED 2026-07-08** (backfill run, see Resolved below). Original detail retained for reference: **`price_per_unit` kit-vial-count factor — write-path fix shipped 2026-07-03; one-time all-vendors backfill still pending.** All three write paths (`price_import.php`, `prices/update.php`, `products/spec_update.php`) compute $/mg through a shared `pricePerUnit()` helper in `helpers.php` = `price_usd / (kit_vial_count * numeric_value)`, zero-denominator guarded; `prices/update.php` also recomputes when `kit_vial_count` alone changes. An admin per-vendor **Recalculate $/unit** button (`POST /vendors/{id}/recalc-prices`) is live on the Inventory tab. **Remaining:** every price row written before the fix still holds the old inflated $/unit until its vendor is recalculated, or run once: `UPDATE pc_prices pr JOIN pc_specifications s ON s.id = pr.specification_id SET pr.price_per_unit = ROUND(pr.price_usd / (GREATEST(pr.kit_vial_count,1) * s.numeric_value), 6) WHERE s.numeric_value > 0`. (User is handling the backfill.) See [[wiki/analyses/2026-07-03-blueprint-vs-actual]].
3. ~~**Price history isn't real**~~ — **RESOLVED 2026-07-03** (see Resolved below). Calendar UI rebuild to actually surface this data is backlog #15.
4. **Stripe billing** — annual tiers $50 / $140 / $340. Before billing is built: referral credits (`pc_referral_credits.amount_usd`) must change from a dollar payout to crediting the referrer N months of service at their current tier. Settings page.
5. ~~**Export — tied to paid tiers.**~~ — **RESOLVED 2026-07-04** (see Resolved below).
6. ~~**Claude extraction pipeline**~~ — **built** (`backend/api/files/process.php`, `backend/lib/claude.php`).
7. ~~**Post-deploy health check**~~ — **RESOLVED 2026-07-03** (see Resolved below).
8. **ClamAV scan — built, but disabled in prod. ON HOLD (root cause known, external blocker).** `backend/lib/malware_scan.php` is wired into `backend/api/vendors/files.php` behind `MALWARE_SCAN_ENABLED` (`false` on prod). **Root cause (confirmed by user 2026-07-08):** `freshclam` can't download virus signatures — the ClamAV CDN (`database.clamav.net`) blocks this AWS/EC2 IP range, so `clamd@scan.service` has no signature DB and crash-loops on start (the "clamd@scan restart failed" warning deploy.sh prints every run). This is NOT a code/daemon-config bug to root-cause locally — it's an external download blocker. **On hold** until the user finds a way to get signatures onto the box (options to explore when picked up: a signature mirror not on the blocked range, an S3/private-mirror `DatabaseMirror` in `freshclam.conf`, a CVD file copied in out-of-band, or a proxy). Uploads are unscanned until then. Do not re-investigate the daemon itself — the daemon is fine, it just has no DB to load.
9. ~~**Full vendor purge**~~ — **RESOLVED 2026-07-08 as hide-not-delete** (see Resolved below).
10. ~~**Kit/tier sizes above 255 not storable**~~ — **RESOLVED 2026-07-03** (see Resolved below).
11. ~~**Comparison table only ever shows the 1-kit tier**~~ — **RESOLVED 2026-07-03** (see Resolved below).
12. **Vendor file storage coverage is partial** — only `price_list` and `coa` categories are handled end to end; other per-vendor file types/purposes still need coverage (categories not yet specified).
13. ~~**`perf.php` has no rate limit**~~ — **RESOLVED 2026-07-03** (see Resolved below).
14. ~~**No file-level dedup on vendor uploads**~~ — **RESOLVED 2026-07-03** (see Resolved below).
15. ~~**Calendar view doesn't show real price history**~~ — **RESOLVED 2026-07-03** (see Resolved below).
16. ~~**Shopping cart, "Buy This Stack", and classification rework**~~ — **RESOLVED 2026-07-03, all three phases shipped** (see Resolved below). Full spec: [[wiki/analyses/2026-07-03-shopping-cart-and-classification-spec]].
17. ~~**Public-facing Calendar (options 1/2/3)**~~ — **RESOLVED 2026-07-03** (see Resolved below).
18. ~~**Public Calendar — admin-picked featured product**~~ — **RESOLVED 2026-07-08** (see Resolved below). _(Shipped admin-picked per user decision.)_ Original:  **Public Calendar — rotating fully-open featured product** — one product per day gets full real detail (vendor, exact price, delta) shown to anonymous visitors, everything else stays teased (name/spec only, per backlog #17). Deferred from the 2026-07-03 public-calendar build as its own decision — needs a rule for which product gets featured (most-changed? random? admin-picked?).
19. ~~**Public Calendar — milestone callouts**~~ — **RESOLVED 2026-07-08** (see Resolved below). Original:  **Public Calendar — milestone callouts** — public-facing highlights like "All-time low recorded for Semaglutide today," computed by comparing a new price against that product/spec's full `pc_price_history` range. Deferred from the 2026-07-03 public-calendar build.
20. ~~**Slow-query triage — export tool + root-cause pass**~~ — **RESOLVED 2026-07-04** (see Resolved below).
21. ~~**Vendor contact card**~~ — **RESOLVED 2026-07-04** (see Resolved below).
22. ~~**Raw/bulk powder pricing (sold by the gram, not a finished vial)**~~ — **RESOLVED 2026-07-04** (see Resolved below).
23. **PWA support (manifest, icons, service worker, iOS installability)** — the app was originally meant to be installable on iOS. Nothing exists today: no `manifest.json`, no icon files (even the `favicon.svg` `index.html` already links to has never existed — confirmed live, that path and `/manifest.json` both currently 200 with the SPA shell's HTML instead of a real asset or a 404, since `public/.htaccess` only serves a path directly when a real file exists there). **Full spec drafted 2026-07-04**, not yet built — blocked on the user producing icon assets (navy `#0E2245`/gold `#C8A227`, per the existing design system) before implementation. Full spec: [[wiki/analyses/2026-07-04-pwa-spec]].
24. ~~**Claude API call log**~~ — **RESOLVED 2026-07-05** (see Resolved below).
25. ~~**Extraction scope was peptides-only, silently dropping everything else vendors price**~~ — **RESOLVED 2026-07-05** (see Resolved below).
26. ~~**Claude API admin tab + manual JSON processing**~~ — **RESOLVED 2026-07-05** (see Resolved below).
27. ~~**Files-page notes truncation + full Review Queue clearance + duplicate-product cleanup**~~ — **RESOLVED 2026-07-05** (see Resolved below).
28. **Open product-identity questions surfaced during cleanup, not resolved** — deliberately left untouched pending your call: is "Adipotide" the same product as "Adipotide/FTPP" (3 products currently)? Is "Gonadorelin" the same as "Gonadorelin Acetate" (salt-form question)? Is "Sermorelin" the same as "Sermorelin Acetate"? Separately, dose-baked-into-product-name has grown to **two clusters**: `L-Carnitine`/`L-Carnitine 600mg`/`L-Carnitine 1200mg`/**`L-Carnitine 500mg` (new 2026-07-06)** (4 products), and a newly-noticed cluster from the same pattern — `SU-400 400mg/ml, 10ml`, `Sustanon 250mg/ml, 10ml`, `Supertest 450mg, 10ml`, `TESTOSTERONE CYPIONATE 250mg/ml, 10ml` all carry their dose as literal text in `canonical_name` instead of a spec. Both are a modeling fix (move doses to specs on one product each), not a simple merge — same reasoning as before, still your call. Also unresolved: is `Lipo-C[FAT BLASTER]`/`Lipo-C[FOCUS]` the same product as `Lipo-c` (mapped as new specs on product 33 during 2026-07-06 cleanup, based on the existing catalog already modeling Lipo-C sub-variants this way — reasonable inference, not a certainty) and is `SUPER SHRED` (553mg/ml) the same blend as `SHR - Shred Blend` at a different dose, or a distinct formulation — left pending in the Review Queue (id 2487), not guessed.
29. ~~**Claude API usage limit**~~ — **RESOLVED 2026-07-06** (see Resolved below).
30. **Review Queue duplicate-naming cleanup, ongoing pattern** — every vendor-file import surfaces some `new_product` rows that are actually existing catalog products under that vendor's own bare/abbreviated wording (no real alias row existed to match against). Cleaned up again 2026-07-06 (see Resolved below) — 44 real aliases added this pass to reduce recurrence for future vendors selling the same items, but this will keep happening as new vendors onboard with their own naming conventions; not something to "finish," just keep doing per-import.
31. ~~**Async PDF queue has no claim step**~~ — **RESOLVED 2026-07-08** (see Resolved below).  
   _(original:_  — `cron/process_async_queue.php` selects every large PDF at `processing_status='processing'` and runs extraction (up to 400s each) with no claim/lock, so two overlapping cron ticks can process the same file twice: double Claude spend, double price-history writes. Fix: per-row conditional claim (`UPDATE ... SET processing_status='processing_claimed' WHERE id=? AND processing_status='processing'`, check rowCount) before extracting. Found in the 2026-07-07 full code review.
32. ~~**Quarantine placement + path inconsistency**~~ — **RESOLVED 2026-07-08** (see Resolved below).  
   _(original:_  — `quarantineFile()` (`malware_scan.php`) puts flagged files in `storage/vendor_files/quarantine/`, which `admin/backup.php` recursively zips — malware samples ship inside every backup. And `vendors/files.php` stores the quarantined row's `stored_path` as an *absolute* path while normal rows are relative, so `files/download.php`'s `storage/ . stored_path` concat breaks for those rows. Move quarantine outside `vendor_files/`, store the path consistently. Found in the 2026-07-07 full code review.
33. ~~**Admin audit log cascades away with its admin**~~ — **RESOLVED 2026-07-08** (see Resolved below). Original:  **Admin audit log cascades away with its admin** — `pc_admin_audit_log.admin_id` is `ON DELETE CASCADE`; deleting an admin account erases their entire audit trail, defeating the log's purpose. Should be `SET NULL` (nullable column) or `RESTRICT`. Found in the 2026-07-07 full code review.
34. ~~**CSV export formula injection**~~ — **RESOLVED 2026-07-08** (see Resolved below).  
   _(original:_  — `comparison/export_csv.php` writes product/spec/vendor names (vendor-file-sourced) raw via `fputcsv`; a name starting with `=`/`+`/`-`/`@` becomes a live formula when the CSV opens in Excel. Prefix-escape those cells. (XLSX export writes string cells, already safe.) Found in the 2026-07-07 full code review.
35. ~~**HTML injection in transactional emails**~~ — **RESOLVED 2026-07-08** (see Resolved below). Original:  **HTML injection in transactional emails** — `email.php`'s `loadTemplate()` does raw `str_replace` of `{{name}}` etc.; display names aren't escaped, so HTML in a display name lands live in the email body. One `htmlspecialchars()` over the vars. Low impact (mostly self-targeted). Found in the 2026-07-07 full code review.
36. ~~**Download filename header quoting**~~ — **RESOLVED 2026-07-08** (see Resolved below). Original:  **Download filename header quoting** — `files/download.php` puts `original_filename` unescaped inside `Content-Disposition: filename="..."`; a `"` in an uploaded filename mangles the header (no CRLF risk — PHP blocks that). Use `filename*=` with `rawurlencode` or strip quotes. Found in the 2026-07-07 full code review.
37. ~~**Admin self-demotion lockout guard**~~ — **RESOLVED 2026-07-08** (see Resolved below).  
   _(original:_  — `admin/users_show.php` lets the last remaining admin remove their own `is_admin` flag with no guard; one bad click locks everyone out of `/admin`. Add a last-admin check. Found in the 2026-07-07 full code review.

## Resolved

- **Backlog batch #18, #19, #30(MT), #33, #35, #36 (2026-07-08, second session that day)**
  - **#18 public-calendar featured product (admin-picked, per user decision)**: new `pc_calendar_features` table (migration `025`, one product/day, optional pinned spec + note). Admin endpoint `admin/calendar-features` (GET month list / POST upsert / DELETE by date) + a new admin "Calendar" tab (`CalendarTab.vue`) to pick date→product→spec→note. Public `calendar_public.php` resolves the featured day to the product's cheapest current active listing (vendor, price) plus a delta from its latest `pc_price_history` row, and `CalendarView.vue` renders it as a full card (gold ★ on the day cell) for logged-out visitors only — everything else stays teased. Verified live: a test feature on Melanotan 1 resolved to Premipeptides 10mg $27.79 with note intact.
  - **#19 all-time-low milestones**: `calendar_public.php` now also returns `milestones` — for each product+spec that changed in the month, flags the day it first hit a new recorded low (requires a prior higher price, so a single-datapoint pair doesn't count). Rendered as name-only 🏆 callouts (no vendor/price, preserving the teaser). Verified live: milestones populated across real July history.
  - **#30 MT→Melanotan 1**: merged product 321 "MT" (SKU MT1) onto Melanotan 1 (93) per your decision — `migration_scripts/2026-07-08-merge_mt_into_melanotan1.php`. Cagrisema left merged into Cagrilintide+Semaglutide per your decision (no action). Catalog now 199.
  - **#33 audit log outlives its admin**: migration `024` — `pc_admin_audit_log.admin_id` made nullable, FK switched `CASCADE`→`SET NULL`. Deleting an admin now keeps their 3,000+ audit rows (admin_id NULL) instead of erasing them. Verified live (DELETE_RULE=SET NULL, nullable).
  - **#35 email HTML injection**: `email.php` `loadTemplate()` now `htmlspecialchars()`-escapes every substitution except the trusted self-built `button`. Verified: a `<script>` display name escapes, button HTML passes through.
  - **#36 download filename header**: `files/download.php` now sends an ASCII-sanitized quoted `filename` (quotes/control chars stripped) plus RFC 5987 `filename*=UTF-8''<percent-encoded>` for the real name. Verified: an embedded `"` no longer breaks the header.


- **Backlog batch #2, #9, #30, #31, #32, #34, #37 (2026-07-08)** — one session after the 2026-07-07 full code review.
  - **#2 price_per_unit backfill**: ran the canonical recompute across all rows. Dry-run found exactly 30 stale rows (Premipeptides June-Sale remnants + CALLA rows written before their spec `numeric_value` was corrected); all 30 fixed, 0 stale after, pricing cache busted. Archived as `migration_scripts/2026-07-08-price_per_unit_backfill.sql`.
  - **#9 vendor purge as hide-not-delete**: new `pc_vendors.is_hidden` column (migration `023_vendor_hidden.sql`). Hiding forces `is_active=0` (which every user-facing query already filters on, so it disappears from comparison/cart/exports) AND drops the row from the admin vendor list, unlike plain deactivate. Admin list gains a "Show hidden" toggle (`?include_hidden=1`) for audit/unhide and a per-row Hide/Unhide button; `updateVendorScalarFields()` handles the flag + the implied `is_active=0`. All data (prices, files, history) is kept — never a real delete.
  - **#30 ongoing duplicate cleanup**: the overnight Premipeptides reprocess + a new "Peptide Research Solutions" vendor created 23 products (ids 320–342) that were existing catalog products under vendor-specific/dose-in-name wording. Confirmed 22 case-by-case against each winner's existing specs and merged them (via `migration_scripts/2026-07-08-merge_overnight_import_duplicates.php`, same logic as `products/merge.php` incl. the new cart/stack repoint) — catalog 222 → 200, 0 orphaned prices/cart rows. Left **id 321 "MT" (SKU MT1) pending, flagged not guessed** — Premipeptides lists MT-2 separately (was 320), so "MT" is probably Melanotan 1, but ambiguous enough to leave for the user's call. Cagrisema (332/333/334/336) mapped onto the existing generic "Cagrilintide + Semaglutide" product (60) — a trade-name-vs-generic identity call, noted here in case you'd rather it be its own product.
  - **#31 async queue claim**: `cron/process_async_queue.php` now takes a `GET_LOCK('pc_process_async_queue', 0)` before scanning — a second overlapping tick no-ops (logged as skipped) instead of double-processing the same large PDF. GET_LOCK over a schema claim column on purpose: the lock auto-releases on connection close, so a crashed run can't leave a file stuck claimed.
  - **#32 quarantine**: `quarantineFile()` now writes to `storage/quarantine/` (OUTSIDE `storage/vendor_files/`, which `admin/backup.php` zips — malware no longer ships in backups) and returns a path relative to `storage/` like every other `stored_path`, fixing the absolute-vs-relative inconsistency that broke `files/download.php` for quarantined rows. No existing quarantined files on prod to migrate (flag's been off).
  - **#34 CSV formula injection**: `comparison/export_csv.php` now prefixes any cell starting with `= + - @` with a single quote (standard Excel defusal) — applies to header + product/spec/vendor name cells; numeric price cells untouched.
  - **#37 last-admin guard**: `admin/users_show.php` refuses to clear `is_admin` on the only remaining admin (422), so an admin can't lock everyone out of `/admin`. Prod currently has 2 admins.


- **Claude API usage limit blocker (2026-07-06)** — backlog #29. You raised the org's $10/month spend limit in the Anthropic console (was at 100% used, resetting Aug 1) after I found it was a self-configured spend cap, not a credits-exhausted or rate-limit issue. Vendor file 32 (`Lucy-Oil Updated List.pdf`) reprocessed successfully afterward: 3 rows matched exactly and imported straight to `pc_prices`, 64 sent to the Review Queue, 0 recurrence of the alias-echo naming bug (rule 7 fix from earlier the same day held).
- **Review Queue cleanup, file 32/Lucy-Oil batch (2026-07-06)** — went through all 64 pending rows from the reprocessed file. 52 were `new_product`; cross-referencing each against the existing catalog (not just the fuzzy matcher's candidate) found **46 were actually existing products** under this vendor's own bare/abbreviated wording that didn't exact/fuzzy-match (e.g. `"BLEND 300"` vs. the catalog's `"B300 (BLEND 300, Trenbolone Acetate 100mg + ...)"`) — confirmed case by case, not guessed (e.g. `"SHRED"` and `"Lipo-C[FOCUS]"` confirmed via an *exact* existing spec-dose match on the target product; `"SUPER Human Blend"` confirmed via the target product's own creation history in `pc_pending_imports.raw_json`, which showed an earlier vendor's typo'd `"SuperHuanBlend"` landing on the same product). Mapped all 46 onto their real existing product via a script replicating `pending_imports.php`'s exact approve logic (`migration_scripts/2026-07-06-review_lucy_oil_batch.php`), then added each as a **real alias row** (`migration_scripts/2026-07-06-add_lucy_oil_aliases.php`, 44 added) so the *next* vendor using similar bare wording matches automatically instead of repeating this manual pass.
  - **Real identity bug caught before approving**: pending row 2484 (`"L-Carnitine 500mg"`) had a stale `candidate_product_id` pointing at the existing `"L-Carnitine 600mg"` product from extraction-time fuzzy matching (`"500"` vs `"600"` is a 1-character Levenshtein distance) — approving normally would have silently recorded a 500mg price under the 600mg product. Forced it to create its own distinct product instead (id 315) — same class of bug as the 2026-07-05 Dulaglutide/Liraglutide/Semaglutide case.
  - **4 genuinely new products** approved normally: `GHK-CU Blend`, `L-Carnitine + Vitamin Blend`, `Relaxation PM`, `Vitamin D3`.
  - **1 row deliberately left pending**, not guessed: `"SUPER SHRED"` (553mg/ml) — no existing spec confirms whether it's `SHR - Shred Blend` at a different dose or a distinct formulation.
  - **Two dose-baked-into-product-name clusters surfaced**, extending backlog #28: `L-Carnitine` now has 4 separate products by dose instead of 1 product with 4 specs; a newly-noticed cluster (`SU-400`, `Sustanon`, `Supertest`, `TESTOSTERONE CYPIONATE`) has the same flaw. Approved the matching new rows onto the existing (imperfectly-named) products since that's functionally correct — the cosmetic rename/remodel is still your call, not blocking.
  - Verified live: 1 pending remains (the deliberately-flagged row), 199 total products (up from 194 + 5 genuinely new), repeat of the whole-catalog substring duplicate scan shows nothing new needing action beyond the already-known backlog #28 cases.

- **Files-page notes truncation + full Review Queue clearance + duplicate-product cleanup (2026-07-05)** — user asked to (1) go through the full Review Queue and (2) fix the Files tab so notes truncate to 255 chars with a click-to-expand card instead of showing the raw full text inline.
  - **Notes truncation**: `FilesTab.vue`'s notes cell now renders `truncateNotes()` (255 chars + `…`) as a clickable button; clicking opens a small modal (`notesFile` ref, reusing the existing `.view-backdrop`/`.view-card` pattern) showing the full `processing_notes` text. Verified live: 337-char note truncated to 256 chars inline, full text in the modal.
  - **Review Queue, 371 pending items processed to 0**: found the user had already manually reviewed part of the queue that morning (products 158–193, ~11:04–11:26) before asking me to continue. Rather than one-by-one browser clicks (371 items), replicated `pending_imports.php`'s exact approve logic in a script run via `sudo -u apache php` — same code path, same `logAdminAction()` audit trail, just a reliable transport.
  - **Real identity bug caught before approving anything**: 10 `name_mismatch` rows had fuzzy-matched **Dulaglutide** and **Liraglutide** onto **Semaglutide** — three genuinely different GLP-1 drugs (same class, different molecules, like Ozempic vs. Victoza vs. Trulicity) that only *look* similar because they share the "-glutide" suffix and landed within the fuzzy matcher's edit-distance-3 threshold. Approving with the UI's default "map to candidate" would have merged three real drugs' prices into one. Forced these to create their own distinct products instead.
  - **Duplicate-naming clusters found and consolidated before approving**: scanning the 158 `new_product` names surfaced 9 clusters where the same real product was listed under inconsistent casing/wording across different vendor files (e.g. "HHB" / "Healthy Hair Skin Nails Blend" / "HHB - Healthy Hair skin nails Blend"). Approved one row per cluster normally, forced the rest onto that same resulting product id — avoided creating ~24 avoidable duplicate products.
  - **11 rows failed on a real data-length bug**: some multi-ingredient wellness blends (Lipo-C, MIC blends, KLOW) had Claude's extracted `spec_label` carrying the *entire ingredient breakdown* (up to 156 chars) instead of a short dose string, exceeding the `VARCHAR(50)` column. Fixed by truncating to fit (47 chars + `...`) rather than widening the schema — a 50-char column is correctly sized for every other product on the platform; these blends are the outlier.
  - **Broader duplicate-product audit, found independently of the queue review**: cross-referencing the newly-created products against the *existing* catalog (not just against each other) surfaced a systemic, pre-existing pattern — Claude's extraction sometimes bakes known aliases into `canonical_name` itself (e.g. `"AOD-9604 (AOD9604)"`), which defeats exact-match against the real existing product (`"AOD-9604"`) and silently creates a duplicate. A substring self-join scan across the whole `pc_products` table found 35 such pairs (including a 3-way split on **KLOW** across products created on three different days this week). Merged all 35 via the existing `products/merge.php` logic (moves specs/prices/aliases onto the winner, keeps the loser's name as a new alias). Total catalog: ~219 products down to **184** after both merge passes.
  - **Deliberately not merged** — flagged as backlog #28 for the user's own call rather than guessed: `Adipotide` vs. `Adipotide/FTPP` (3 products, unclear if the same formulation), `Gonadorelin` vs. `Gonadorelin Acetate`, `Sermorelin` vs. `Sermorelin Acetate` (salt-form questions, pre-existing), and `L-Carnitine`/`L-Carnitine 600mg`/`L-Carnitine 1200mg` existing as three separate products instead of one product with two specs (a modeling fix, not a merge). Confirmed via a repeat of the substring scan that everything else remaining is a genuine false-positive (combo products like `Cagrilintide + Semaglutide`, or genuinely distinct compounds like `FOXO4` vs. `FOXO4-DRI`, `HGH` vs. `HGH Fragment 176-191`) — nothing else needs action.
  - Verified live end-to-end: 0 pending remain (`SELECT status, COUNT(*)` confirms), a live Comparison search for "KLOW" shows all 4 vendor rows under one consolidated product, and a repeat of the duplicate scan turns up only the deliberately-excluded cases above.
- **Claude API admin tab + manual JSON processing (2026-07-05)** — follow-up the same morning: a new "Claude API" admin tab (`ClaudeApiTab.vue`) surfaces the live extraction/vendor-contact-parse system prompts (`GET /admin/claude-prompt`) and a browsable call-log table (`GET /admin/claude-log`, `GET /admin/claude-log/{id}` for the full raw response) reading from the `pc_claude_call_log` table built earlier that session — no more needing direct SQL to see what Claude actually said.
  - **Manual JSON processing**: refactored `processVendorFile()` (`backend/lib/vendor_file_processor.php`) to split out `commitExtractionResult($file, $result)` — the entire commit/pending-import logic, now shared by the real Claude path and a new manual path. New "Manual JSON" button on the Files tab opens a paste-in textarea; `POST /files/{id}/manual-process` validates the pasted text decodes to an object with a `prices` array, then runs it through the *exact* same commit logic a real "Process" click uses — lets the admin substitute output from another tool (Grok, hand-corrected JSON) without ever calling Claude, and without any risk of the manual path drifting from the real one since they're now literally the same function.
  - Verified live end-to-end: Claude API tab renders the current (broadened-scope) prompt and all 45 real logged calls with working detail view; a real manual-JSON submission on the Scarlett file correctly sent a test row to the Review Queue and updated the file's status/notes identically to a real Claude-processed file (test row cleaned up after).
- **Full 26-file vendor reimport, Claude API call log, and extraction scope broadening (2026-07-05)** — user asked to reimport every uploaded vendor `price_list` file (26 files across 13 vendors) to verify recent fixes, run overnight while unattended, no Review Queue approvals from me at any point (left entirely for the user).
  - **Claude API call log (backlog #24)**: new `pc_claude_call_log` table (migration `022_claude_call_log.sql`) — persists every raw Claude API call (extraction + vendor-contact-parse): vendor file id, call type, model, `http_status`, `stop_reason`, full token usage (input/output/cache-creation/cache-read), the raw response text, whether it parsed as valid JSON, and the error message if not. Hooked into `callClaudeMessages()` (`backend/lib/claude.php`) at all three exit points (HTTP error, JSON-parse failure, success) via a best-effort `logClaudeCall()` helper that swallows its own exceptions so a logging failure can never break real extraction. Motivated directly by having to spend a fresh, costly API call mid-session just to see why a file's extraction failed, since only the first 300 characters of a failed response were ever kept before this.
  - **Extraction scope broadened from peptides-only to everything priced (backlog #25)**: user caught that the system prompt's opening line — `"You are a peptide vendor price list parser"` — was causing Claude to self-select and silently omit non-peptide rows (steroids/hormones like Trenbolone/Drostanolone/Masteron/Superdrol, vitamin/cosmetic blends like Lipo-C/HHB), even though nothing in the schema or classification system actually restricts products to peptides. Rewrote the prompt to extract every priced row regardless of category. Verified live post-fix: real steroid blends and cosmetic blends now land in the Review Queue instead of vanishing.
  - **Real root-caused bug found via the new call log**: `Peptide0616.xlsx` (Purelypep Factory, ~399 rows) failed extraction with "not valid JSON" — the call log showed `stop_reason: max_tokens` at exactly the `48000`-token ceiling, confirmed as a genuine truncation once the broadened scope made this file's response larger (not the probabilistic flakiness first suspected on an earlier, narrower-scope failure of the same file). Fixed by raising `max_tokens` to `64000`; the retry succeeded at `50103` output tokens.
  - **Full reimport execution**: all 26 files processed under the final (broadened-scope, raised-ceiling) configuration — **26/26 succeeded, 0 failures**, 45 total Claude calls logged (only 1 failure across the whole log, the max_tokens case above, before the fix). Several files were reprocessed twice same-night (once before the scope broadening, once after) to guarantee full consistency, per the user's explicit choice. New vendors/files uploaded mid-session by the user (Golden Age, CALLA — 10 photos, Guangzhou Guangjin Trading, protidexbio, NOVI OREA, Premipeptides, Tidetron Peptide) were included in the same pass. Final state: 794 items sitting in the Review Queue (untouched, for the user), 2,173 active prices, 109 products (up from 105).
  - **Process notes**: browser-tab automation was unreliable for a long unattended batch (repeated CDP freezes requiring fresh tabs) — switched the bulk of the run to a small script (`reimport_batch.php`) that calls the exact same `processVendorFile()` function the real "Process" button uses, run via `sudo -u apache php` (matching the real file-ownership permissions) in the background with `nohup`/`disown` so it survives independent of any single SSH session — not a shortcut around the real logic, just a more reliable transport for the identical code path, including the same `logAdminAction()` audit trail.
  - Verified live: Review Queue count (794) matches exactly; spot-checked real steroid/blend rows now present; `pc_claude_call_log` confirmed populated correctly throughout.
- **Raw/bulk powder pricing (2026-07-04)** — user surfaced a genuinely new vendor-file shape: a vendor ("Scarlett," new, uploaded as `scarlett_raw_2025.xls`) selling raw, unprocessed peptide powder priced per gram (`$/G`), not a finished vial — no kit/vial count, no Cat No, just compound name + flat per-gram price, 58 rows.
  - **Design decision (user confirmed)**: same product entity as the vial/finished form (e.g. "Semaglutide" raw powder and "Semaglutide" 5mg vial are one product), distinguished by a new **spec** and a new **classification tag**, not a separate product per raw compound — avoids catalog duplication and reuses the classification multi-select filter already built.
  - **Schema needed zero changes**: `pc_specifications.numeric_value` was already documented as normalizing grams to mg. A raw-powder row is `spec_label="1g"`, `numeric_value=1000`, `unit="mg"`, `kit_vial_count=1`, `tier_kit_size=1` — the existing $/mg math, Comparison table, cart, and stacks all work unmodified.
  - **Extraction prompt** (`backend/lib/claude.php`, `buildExtractionSystemPrompt()`): added rule 10 — recognizes "$/G"/"price per gram"/"bulk"/"raw powder" listings and emits the `1g` spec shape above. Deliberately did *not* try to model hypothetical multi-tier bulk pricing (1g/10g/100g at different rates) since no real file has shown that shape yet — a source with multiple weight breaks still gets every row extracted, just with a warning flagging it for human review rather than guessing the right modeling.
  - **First-pass classification approach, superseded same day**: a "Raw Material" classification tag (id 273), created through the real admin UI and bulk-applied via direct SQL — but the user caught that this was product-level, so filtering by it surfaced *every* spec of a tagged product (vial sizes included), not just the `1g` row. Fixed by migration `021_raw_material_spec.sql`: added `pc_specifications.is_raw_material BOOLEAN`, backfilled `1` for every existing `1g` spec, and retired the classification tag entirely (deleted both the `pc_product_classifications` rows and the tag itself — it never served a purpose the spec-level flag doesn't). `findOrCreateSpec()` (`price_import.php`) gained an `$isRawMaterial` parameter; the extraction prompt's rule 10 now emits `is_raw_material` directly on each row instead of leaving it to be inferred from `spec_label`; `pending_imports.php`'s approve action and the Review Queue UI (a new "Raw/bulk powder" checkbox, mirroring `non_standard_kit`) both thread it through. `runComparisonQuery()`/`parseComparisonFiltersFromGet()`/`comparison/index.php` gained a `rawMaterialOnly` filter (`s.is_raw_material = 1`, not a classification join) wired to both export endpoints and the query-log rerun tool. Frontend: replaced reliance on the classification pill with a real "Raw/bulk powder only" checkbox (same pattern as "Verified vendors only"), plus a small "Raw" badge on the spec column so it's visible even with the filter off.
  - **Import**: vendor "Scarlett" created; the source `.xls` (legacy binary format, not accepted by the upload endpoint — only pdf/xlsx/csv/jpg/png/zip) converted to CSV and processed through the real pipeline. All 58 rows extracted correctly (1 zero-priced row correctly skipped per the existing "blank/zero price" rule); all 57 priced rows landed in the Review Queue as `new_spec` (since no product had a `1g` spec yet) and were approved through the real Review Queue UI.
  - **Real bugs hit and fixed during the live import** (not code bugs — process/tooling issues):
    1. Placed the uploaded file on the server directly via SSH+PHP (browser file-upload tool wasn't accepting host paths in this environment) — the directory got created owned by `ec2-user:ec2-user` instead of `ec2-user:apache`, so PHP-FPM (running as `apache`) got permission-denied and the file processed as "missing from disk." Fixed with `chgrp -R apache`.
    2. A rapid-fire batch of Review Queue "Approve" clicks outpaced the server (each approval round-trip ~6-8s) and likely double-submitted on a stale card, hitting a 404 that triggered a blocking `alert()` — which froze that browser tab entirely (matches "renderer unresponsive" CDP errors exactly, including a keyboard-dismiss attempt timing out the same way). Recovered by abandoning the frozen tabs and continuing on a fresh one, this time waiting for real confirmation (the "Remaining N pending" count actually decreasing) before each next click instead of a fixed delay.
  - Verified live end-to-end: all 57 rows committed (0 pending, 0 rejected, no duplicates — 57 distinct `1g` specs), correct $/mg math (`Semaglutide 1g @ $128.34 → $0.13/mg`). Post-fix: toggling "Raw/bulk powder only" returns **exactly 57 rows**, every one showing the "Raw" badge — no vial specs mixed in (confirmed the earlier product-level-tag version showed 197 rows, vial sizes included, before the spec-level fix). Unchecking the filter restores the full unfiltered set. Vendor "Scarlett" shows as a real column.
- **Vendor contact card (2026-07-04)** — clicking a vendor's name (Comparison table vendor header, Cart's "cheapest vendor" list) opens a card with their contact details: WhatsApp, phone(s), email, Discord, Telegram, website, shipping note, verified badge. Available to any authenticated user, not tier-gated or admin-only — contacting a vendor is core to the product.
  - New `GET /vendors/{id}/contact` (`requireAuth()`, not `requireAdmin()`) — deliberately excludes the admin-only internal `notes` field and everything else `vendors/show.php` exposes (files, full price list).
  - WhatsApp is a `wa.me` deep link pre-filled with "I found you on Price TheMightyGroupBuy, I would like to know more about: ", plus a comma-separated list of Cat Nos for any cart items that vendor carries (falls back to the product/spec label when a row has no vendor SKU). `getCartSnapshot()` (`backend/lib/cart.php`) extended to carry each covered item's `vendor_sku` per vendor for this.
  - New reusable `frontend/src/components/VendorCard.vue` modal, wired into both `ComparisonView.vue` (vendor column header, now a button) and `CartView.vue` (vendor name in the cheapest-vendor list).
  - **Real bug caught during live verification**: one vendor's `whatsapp` field held two numbers separated by `/` (`"+852 111/+852 222"`); stripping all non-digits before splitting glued both into one invalid garbled number. Fixed by splitting on `/`/`,` first and using only the first number. Verified live against two real vendors — one single-number (correct dialable link + Cat Nos from a real cart), one two-number (confirmed post-fix it resolves to just the first number).
- **Slow-query triage — export tool + root-cause pass (2026-07-04)** — user asked to work through the 408 rows sitting in `pc_slow_query_cache` (all `status='new'`), offering a choice between an export tool and a direct investigate-and-fix pass; chose both.
  - **Export**: `GET /admin/slow-queries/export` (CSV, admin-only, optional `?status=` filter, all columns, no `LIMIT 100` unlike the list endpoint), "Export CSV" button added to the System tab's slow-queries toolbar. Verified live (real download).
  - **Real root cause found and fixed**: the ranked list was mostly noise — the top two "slowest" entries (12.4s each) were the collector's own maintenance queries, permanently pinned by a one-time historical spike (`query_time_secs` uses `GREATEST(old,new)`, never decays); one entry was a harmless external heartbeat (`SLEEP()`). Beneath that noise, 91 of the 408 rows (122,077 of 135,441 total logged occurrences — 90%) were all the same query shape: `findExactProductMatch()` (`backend/lib/price_import.php`, the product/alias resolution used on every price line during vendor import and review-queue approval) wrapped `canonical_name`/`alias` in `LOWER()`, which defeats their UNIQUE indexes even though both columns already use case-insensitive (`utf8mb4_unicode_ci`) collation — a plain `=` already matches case-insensitively. Confirmed via `EXPLAIN`: `LOWER()` version scanned the full index (~105 rows, `type=index`); removing it dropped to a `const` unique lookup (1 row). Verified correctness on the live server via a real PHP call (`'bpc-157'` and `'BPC-157'` both resolve to the same product id; a non-existent name still correctly returns `null`). Fixed by removing the `LOWER()` wrapping — deployed and verified live.
  - **Triage**: all 408 rows marked via direct DB update (not the admin PATCH API, per explicit instruction to "go get them from the db and mark them yourself") — 94 `resolved` (91 for the fixed bug + 2 collector-noise + 1 heartbeat, each with an explanatory `status_note`), 314 `acknowledged` (reviewed — proportionate to real usage and already using available indexes at this dataset's size, e.g. the Comparison query and admin list queries; no changes needed). Verified live in the admin System tab (94 resolved / 314 acknowledged counts match exactly).
- **Export, all three formats (2026-07-04)** — backlog #5, built after the user sourced `php_xlsxwriter` into `backend/lib/xlsxwriter.class.php`:
  - **CSV** (`GET /comparison/export/csv`, Pro tier+): stdlib `fputcsv()`, one column per vendor (price only, per the spec's CSV-is-the-simpler-format decision), plus Avg/Median.
  - **XLSX** (`GET /comparison/export/xlsx`, Pro tier+): full blueprint formatting — merged two-row vendor headers (navy `#2F5496`/blue `#4472C4`), alternating row shading, a slightly-darker italic `$/unit` column, green `#C6EFCE` highlight on the lowest-$/unit cell pair, frozen panes at C3, exact column widths.
  - **Full dataset JSON** (`GET /export/full`, Expert tier): every active product/vendor/spec/price row, unfiltered — broader than the Comparison view, matches the existing `me/export.php` pattern (no type-casting, raw DB rows).
  - Shared `parseComparisonFiltersFromGet()` added to `comparison_query.php` so the GET-param parsing can't drift between the live view and both export formats (`comparison/index.php` now calls it too instead of duplicating the parsing).
  - Frontend: "Export CSV"/"Export Excel" buttons on the Comparison filter bar (tier-gated, "Export (Pro+)" upsell link otherwise), "Full dataset export" card on Settings (Expert-only). Both use the existing fetch+blob+anchor download pattern already established by `WaitlistTab.vue`/`BackupTab.vue`.
  - **Real bug caught during live verification, fixed same pass**: the vendor Price/$-per-unit/Avg/Median columns were typed `'price'` (forces every cell in that column to the numeric XML branch), but the custom two-row header writes text into those same columns ("HKpep", "Price ($)") — producing invalid `<c t="n"><v>HKpep</v></c>` XML. Confirmed broken with `openpyxl` (`ValueError: could not convert string to float: 'HKpep'`, refused to open the file at all — real Excel would reject it too). Fixed by switching those columns to `GENERAL` type, which auto-detects string vs. number per cell instead of forcing one format for the whole column. Verified after the fix: `openpyxl` opens cleanly, merges/freeze-panes/column widths intact, and the green lowest-price highlight lands on the correct cell (confirmed against real live data — Purelypep Factory at $0.28/mg on 5-Amino-1MQ 5mg).
  - Verified live end-to-end against real production data via the actual UI (not synthetic): CSV (10.8KB, real vendor columns), XLSX (opens clean post-fix, 239 rows), full JSON (105 products / 1016 prices / 9 vendors).
- **`price_per_unit` write-path fix (2026-07-03)** — shared `pricePerUnit()` helper (`helpers.php`) + per-vendor **Recalculate $/unit** button routed through all three write paths; corrected the missing `kit_vial_count` factor and added a zero-denominator guard. Deployed live. The one-time backfill of pre-fix rows remains open — see backlog item 2.
- **Spec sorting bug (2026-07-03)** — `vendors/show.php`'s price query (Inventory tab + Vendors-tab price list) sorted by `spec_label` (string) instead of `numeric_value` (decimal), producing lexicographic order (10, 15, 20, 30, 5, 60mg). The public Comparison table and Products tab were already correct. Fixed and verified against live data same day. Full writeup: [[wiki/analyses/2026-07-03-blueprint-vs-actual]].
- **Post-deploy health check (2026-07-03)** — real `/api/health` endpoint (`backend/api/health.php`): DB round-trip, Memcached set/get, mail config sanity. `deploy.sh`'s `smoke_check()` now curls it and prints per-component pass/fail instead of just pinging homepage + app-settings. Deployed and verified live.
- **Kit/tier sizes above 255 (2026-07-03)** — migration `014_widen_kit_columns.sql` widens `pc_prices.kit_vial_count` and `tier_kit_size` from `TINYINT UNSIGNED` to `SMALLINT UNSIGNED` (max 65,535); `schema.sql` and all three `min(255, ...)` clamp sites (`pending_imports.php` ×2, `vendor_file_processor.php`) plus the two admin-edit range checks in `prices/update.php` updated to match. Deployed and verified live.
- **`perf.php` rate limit (2026-07-03)** — one-line fix, `rateLimit('perf_' . IP, 60, 300)` added, matching every other public write endpoint. Deployed and verified live.
- **Price history (2026-07-03)** — new append-only `pc_price_history` table (migration `015_price_history.sql`, no FKs by design). `commitPriceRow()` (`price_import.php`, the single choke point for all three import call sites) and `prices/update.php` (manual edits) each snapshot the prior row via a shared `logPriceHistory()` helper, only when `price_usd`/`kit_vial_count` actually changed — a no-op reimport or a re-saved identical value logs nothing. New price lines log with `old_price_usd = NULL`. Verified live: a real manual price bump-and-revert logged two accurate rows; the import path was verified via a rolled-back transaction on the server (new-line, real-change, and no-op-resubmit cases all behaved correctly, zero rows persisted from the test). Full spec: [[wiki/analyses/2026-07-03-price-history-spec]]. Calendar UI rebuild to use this data is backlog #15.
- **Shopping cart, "Buy This Stack", classification rework (2026-07-03)** — all three phases of backlog #16 shipped same day:
  - **Classification**: `pc_products.category` (single-select enum) fully retired — `pc_classifications` + `pc_product_classifications` (migration 016, column dropped in 017) replace it, multi-select/OR-filtered, seeded with 27 tags and 112 real product-tag assignments from a user-supplied mapping across two source lists. All 10 real touch points cut over plus a new `GET/POST /api/classifications` endpoint for admin-manageable tags going forward.
  - **Shopping cart**: `pc_cart_items` (migration 018), `GET/POST /api/cart` + `DELETE /api/cart/{id}` (ownership-scoped, 404 on someone else's item), cheapest-single-vendor-covers-cart ranking with a named-missing-items partial-coverage fallback (`"2 of 2 items — missing BPC-157 2mg"`). "+ Cart" button on the Comparison table (moved to the leftmost sticky column same day, per a UI follow-up), new `/cart` page, Dashboard summary card, bottom-nav entry.
  - **Buy This Stack**: `pc_stacks`/`pc_stack_items` (migration 019), full admin CRUD (`admin/stacks/index.php`, `show.php`, `items.php`) plus a new "Stacks" admin tab mirroring the Products tab's pattern, public `GET /api/stacks` + `POST /api/cart/add-stack/{id}` (bulk-add, reuses the cart's exact vendor-breakdown response via a shared `getCartSnapshot()` helper), Dashboard "Buy This Stack" card. Deliberately components-only — no pre-mixed-blend-SKU matching.
  - **UI follow-up same day**: Comparison table's Product column had no width cap either, so an unusually long product name elsewhere in the list was silently stretching it (245px vs. an assumed 140px), which is what was actually pushing the Spec column (and everything after it) too far right — not the Spec column itself. Fixed by bounding all three sticky columns (Cart/Product/Spec) to fixed pixel widths with wrap-instead-of-stretch, which also fixed a latent sticky-scroll misalignment bug that existed before this session's changes.
  - Verified live end-to-end throughout: real API round-trips for cart and stacks (add/dedup/remove/404-on-missing/bulk-add-from-stack), real UI interaction (buttons, chip removal, Dashboard cards reflecting live totals), and DOM-geometry checks confirming the sticky-column fix (zero gap/overlap at rest and mid-scroll). Full spec: [[wiki/analyses/2026-07-03-shopping-cart-and-classification-spec]].
- **Comparison table tier selector + Calendar real price history (2026-07-03)** — backlog #11 and #15, both built same day (Export/#5 deliberately excluded from this pass, still pending the `php_xlsxwriter` library):
  - **Tier selector**: `runComparisonQuery()` gained a `$tierKitSize` parameter (default 1, so old callers/logged queries are unaffected); `comparison/index.php` reads `?tier=`, no fixed-list validation; `comparison/filters.php` exposes the real distinct tiers in the data (`SELECT DISTINCT tier_kit_size`) rather than a hardcoded array. Frontend: a segmented tier control above the Comparison table, only shown when more than one tier exists. Verified live: real data has exactly 3 tiers (1/10/50-kit), switching tiers via a real UI click changed the row count exactly as the API confirmed (174 → 115 rows). Cart/Stack intentionally untouched, stay tier=1-only.
  - **Calendar rebuild**: `calendar.php` rewritten to query `pc_price_history` (LEFT JOIN, no FKs by design, tolerates a since-purged vendor/product) instead of `pc_prices.created_at`. Day-detail table now shows real `$46.00 → $46.01` deltas, a "New" badge for first-time prices, and a source tag (import vs. manual edit) — the "N price changes" count is now genuinely accurate instead of counting every no-op reimport. Verified live against real historical data from an earlier test edit.
  - **Follow-up same day — real gap found and fixed**: the ledger-only rebuild caused a regression the user caught immediately — `pc_price_history` didn't exist yet when a large batch of Review Queue approvals happened earlier that same day (04:21–04:24 AM), so those real, legitimate new-inventory commits have no history row and vanished from the Calendar entirely, even though the underlying `pc_prices` data was always intact (verified: all rows `is_active=1`, all vendors `is_active=1`, no data was ever actually deleted). Fixed by adding a second, independent signal to `calendar.php`: `pc_pending_imports` grouped by `DATE(reviewed_at)` for `status='approved'`, which has always recorded approval timestamps regardless of when the price-history ledger came online. Both signals now render as distinct calendar dots (accent = price changes, green = review-queue approvals) and separate day-detail sections. Verified live: today's dot correctly shows 379 approved items alongside 2 real price changes.
  - Full spec: [[wiki/analyses/2026-07-03-export-tier-selector-calendar-history-spec]].
- **Vendor upload dedup, two layers (2026-07-03)** — backlog #14, expanded past its original file-hash-only framing after discussion surfaced that byte-hash alone misses "different file, same data" re-uploads:
  - **Layer 1 (file hash)**: `pc_vendor_files.content_hash` (SHA-256, migration 020) + a new `skipped_duplicate` processing status. A byte-identical re-upload of a `price_list` file that a prior upload for the *same vendor* already got to `complete`/`skipped_duplicate` is still cataloged (never silently dropped) but marked `skipped_duplicate` immediately — never reaches `pending`, so neither the manual Process button nor the async cron worker ever calls Claude on it. Verified live: uploaded a real test file, marked it `complete`, re-uploaded the identical bytes under a *different filename* and got correctly flagged (`"identical to a previously processed upload"`); a genuinely different file was correctly not flagged. Test rows and orphaned files cleaned up after.
  - **Layer 2 (data-match tally)**: `commitPriceRow()` now returns `bool` (was `void`) — reuses the exact comparison that already decides whether to write a `pc_price_history` row, just exposes it instead of discarding it. `vendor_file_processor.php` tallies changed vs. unchanged rows per file, folded into the existing `processing_notes` field and the process-response message (`"Imported 3 price rows (37 unchanged)."`) — no new schema needed. Verified via a rolled-back transaction on the server: new-line/real-change/identical-resubmit all returned the correct bool.
  - Full spec: [[wiki/analyses/2026-07-03-upload-dedup-spec]].
- **Public-facing Calendar, options 1/2/3 (2026-07-03)** — backlog #17. `/calendar` route no longer requires auth; `CalendarView.vue` branches on `auth.isAuthenticated` — logged-in users keep the full ledger view built earlier that day, anonymous visitors get a new teaser sourced from a genuinely public `GET /api/calendar/public` endpoint (no auth check at all): **(1)** month aggregate — total change count + distinct vendor count, no vendor names; **(2)** classification breakdown weighted by actual change events (a product with 5 changes contributes 5 to its category, not 1; inclusive/OR across a product's tags, same as the Comparison filter); **(3)** per-day teased product/spec names with a "Price changed"/"New listing" label — deliberately no vendor name or dollar amount anywhere in the response, keeping the free-tier quota system's reason to exist intact. Options 4 (rotating fully-open featured product) and 5 (milestone callouts) explicitly deferred to backlog #18/#19. Verified live: hit `/api/calendar/public` with zero auth headers (176 changes/5 vendors/full category breakdown for the month, confirmed no vendor or price fields anywhere), then confirmed the actual rendered page both logged out (summary banner + teased list + "Sign in" CTA) and logged back in (unchanged full-detail view).
  - **Same-session incident, self-inflicted, fixed**: verifying the logged-out view required saving the real session token before clearing it; the save was held in a JS `window` variable that a `location.reload()` between steps wiped, so the "restore" step wrote the literal string `"undefined"` into `localStorage` instead of the real token, crashing the app on that browser tab (`JSON.parse("undefined")` throws, uncaught). Cleaned up the corrupted keys immediately, and hardened `stores/auth.js`'s startup read with a try/catch so any corrupted stored value falls back to logged-out instead of hard-crashing the whole app — closing the exact failure mode this exposed, not just the immediate mess. User had to log back in manually on that tab; only that one browser session was affected, no data or other users involved.

- [[wiki/sources/phase1-framework|Phase 1 Framework Reference]]
- [[wiki/sources/original-project-blueprint|Original Project Blueprint — Founding Prompt]]
- [[wiki/concepts/subscription-tiers|Subscription Tiers]]

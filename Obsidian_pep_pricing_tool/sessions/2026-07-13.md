---
title: Session 2026-07-13
type: analysis
tags: [session]
created: 2026-07-13
sources: []
---

# Session 2026-07-13

Weekly clipping scan (no new files), a Products-tab UI cleanup follow-through, backlog #65/#66, a real deploy-tracking gap fixed, and a pending-imports queue review that drove three same-day fixes. Full detail: [[wiki/analyses/2026-07-13-pending-imports-review]], [[wiki/analyses/2026-07-13-product-cas-mw-followup]].

## Inline "+ new tag" input (rest of #64)

Converted `addNewClassification()`'s "+ new tag" flow to the same inline-input pattern built for aliases the day before, completing backlog #64.

## Search vendors by phone number (#65)

`findVendorByPhone()` already existed (built for parse-intake dedup) and already normalized correctly — added a thin `GET /vendors/find-by-phone` wrapper and a search box next to "+ New vendor". Caught a real bug during verification: the toast store's custom-duration `push()` was never exposed from the composable, so the "not found" toast silently threw instead of showing. Same-day follow-up loosened this to substring search (min 3 digits) via a separate helper — the exact-match version stays as-is for the automated dedup path, where loosening it would risk false-positive vendor matches.

## Pending Imports Queue review

Read-through of the 108-row backlog surfaced several real issues, acted on same day: a with-B12 Lipo-C spec (LC1201) at risk of the same misfiling bug found earlier this week (fixed — moved via `spec_move.php`, found a second already-approved instance of the same miscode while checking), and a likely duplicate ("N-Acetyl Semax Amidate" vs. existing "NA Semax amidate", missed by the fuzzy matcher on wording alone — confirmed same molecule, approved onto the existing product). Also flagged (not yet acted on): a botched extraction, a new Adamax data point, and a genuinely new compound (Eloralintide).

## Skip action on the Review Queue (#66)

Directly motivated by the review above — several rows need real research before a confident approve/reject, but reaching a later row meant deciding every prior one first. Added `last_skipped_at` (migration 031); status stays `pending` so Remaining count is unaffected, but skipped rows sort to the back until their turn comes back around.

## CAS/MW follow-up research

Filled gaps for products added since the 2026-07-12 bulk pass (31 confident values, mostly newer steroid esters) — but first cross-checked the new research pass against the earlier page's more careful conclusions and caught 19 direct conflicts with values already explicitly rejected there, reverting all 19 before they were written. Resolved the open "Adamax" identity question (Semax-adamantane analog, MW 1032.24 — matches the earlier data point exactly).

## Deploy hygiene: `pc_migrations` backfill

Adding the PYUSD payment method surfaced a pre-existing, unrelated gap: migrations 028-031 had all been applied live (via direct API calls/raw SQL during earlier same-week work) but never recorded in `pc_migrations`, so `deploy.sh --sync-schema` aborted on a duplicate-column error. Verified each migration's actual effects were genuinely already live (not just re-running the SQL, which would have failed again), then backfilled the 4 filenames directly. `deploy.sh --all` ran clean end to end afterward.

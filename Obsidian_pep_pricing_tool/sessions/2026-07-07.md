---
title: Session 2026-07-07
type: analysis
tags: [session]
created: 2026-07-07
sources: []
---

# Session 2026-07-07

No commits landed this day — the session was a full read-only code review, with the fixes it produced deployed the next day (2026-07-08).

## Full code review — 2 security holes + 9 correctness/quality findings

Reviewed the whole app surface: router/CSRF/CORS, auth stack, every trust-boundary endpoint (uploads, downloads, public writes), the import/pricing pipeline, merge tools, exports, schema FKs, frontend core. Overall shape held up well — prepared statements throughout, hashed bearer tokens, fail-closed rate limiting, no `v-html`, audited admin actions.

Fixed the next day (items 1-4, user's call to prioritize):
1. Stored XSS via `coa_url` — `FILTER_VALIDATE_URL` accepts `javascript://…`, rendered as a clickable admin-facing link.
2. Same class via `vendor.website` — Claude-extracted from vendor-controlled files, rendered with no scheme validation.
3. `admin/backup.php` merging mysqldump stderr into the dump file itself, corrupting it on any warning.
4. Cart/stack items `ON DELETE CASCADE` to specs/products — every admin merge silently wiped matching user cart/stack rows instead of repointing them.

Backlogged as items 31-37: async-queue claim race, quarantine placement/path inconsistency, audit-log CASCADE, CSV formula injection, email HTML injection, download filename header quoting, admin self-demotion guard.

Accepted by design, confirmed not bugs: the 60s session-cache staleness window on revocation, `perf.php`'s CSRF exemption (rate-limited, no forgeable state), admin-only raw exception text in `message` fields.

Full findings: `log.md` 2026-07-07 entries; fixes for items 1-4 land in [[sessions/2026-07-08]].
